#!/usr/bin/env bash
# Compatibility alias: "ccm" was renamed to "ccprof" in 0.4.0 and will be removed in a future version.
[ -t 2 ] && echo "ccm: this command is now called ccprof, please use 'ccprof' (the 'ccm' alias will be removed)" >&2
exec "$(cd "$(dirname "$0")" && pwd -P)/ccprof" ${1+"$@"}
